# 缠论训练：CZSC 逻辑移植 TODO

## 边界与基准

- [x] 固定参考源码为 CZSC `master` commit `701e480a545004f945bb1721e510ae610ad90c4c`，后续同步上游时必须单独评审并更新来源说明和一致性样本。
- [x] 当前算法基线不会自动追随 CZSC 主分支；后续优化必须作为本项目独立变更评审，不得在数据同步或依赖更新时静默改变训练结果。
- [x] 保持“复制必要算法、拒绝生产运行时依赖”的边界；正式依赖和业务代码不安装或导入 `czsc` 包，仅允许可选测试环境临时加载同版本上游包做差分校验。
- [x] 所有移植代码保留 Apache-2.0 来源、版本和对应源文件说明。
- [x] 删除本项目原有线段近似算法；该 CZSC commit 没有独立线段生成接口，因此不伪造线段结果。
- [x] 只保留当前 CZSC 实现，训练算法不设本项目版本号；追溯依据统一为固定 CZSC commit 和原始信号函数名。

## P0：算法一致性与防未来数据

- [x] 建立 CZSC 上游黄金样本，逐根对比分型、笔和中枢的开始/结束时间、方向、价格及确认时间。
- [x] 为一买/一卖信号 `zdy_macd_bs1_V230422` 建立逐根触发样本。
- [x] 为二买/二卖信号 `cxt_second_bs_V230320` 建立逐根触发样本。
- [x] 为三买/三卖信号 `cxt_third_bs_V230318` 建立逐根触发样本。
- [x] 每个信号保存函数名、版本、参数、触发级别和确认 K 线，禁止只保存“买一/卖一”等无版本标签。
- [x] 增加前缀一致性测试：第 N 根得到的结构和信号不得因第 N+1 根以后的数据发生提前确认。
- [x] 活跃 session 的日线、分钟覆盖、级别统计和质量信息均裁剪到当前已揭示 K 线；完整区间元数据只进入结束记录。
- [ ] 增加真实 A 股复权、停牌、涨跌停、一字板和价格跳空样本。
  - [x] 本地真实停牌间隔、涨停、跌停、一字板和跳空窗口已自动识别并逐根通过 CZSC 差分。
  - [ ] 当前本地没有 `adj_factor` 数据，enriched 价格回退为原始价；待同步真实复权因子后补齐复权变化窗口，审计工具会明确报告该缺口而不会伪造通过。

## P1：增量计算与多级别联立

- [x] 将当前全量重算改为 CZSC 风格的逐根 `update`，只回退并重算受最后一根 K 线影响的未完成结构。
- [x] 日线读取从 1990 年起覆盖本地全部 A 股历史；初始图表仍只展示右侧 60 根，通过缩放和拖动查看此前 K 线。
- [x] 增量缓存 MACD、SMA、RSI、KDJ，避免每揭示一根 K 线重复扫描全部历史。
- [x] 增加性能基准：2,000 根日线的单次更新与 60 步训练总耗时、峰值内存均有固定上限。
- [x] 移植 `BarGenerator` 的交易时段感知合成思想，将本地 1 分钟数据稳定合成为 5/15/30/60 分钟和日线。
- [x] 用同一根 1 分钟输入同时更新 30 分钟与日线分析器；聚合 K 使用稳定周期收盘时间，同周期分钟只替换末根 K，并按训练已揭示日期截断，避免未来分钟进入计算。
- [x] 保存每个级别的 K 线数量、交易日覆盖率、首末数据时间和明确回退原因，并在训练观察卡与历史详情展示。
- [x] 训练记录保存日线训练窗口与可见窗口 SHA-256、行数、首末时间、分钟源与分钟覆盖，作为独立数据版本指纹。

## P1：版本化信号注册

- [x] 建立本项目独立的缠论信号注册表，采用 `名称 + 版本 + 参数` 唯一标识。
- [x] 使用独立信号 profile 组织当前计算；历史训练详情统一按当前代码重算，不保留旧算法分支。
- [x] 参考 CZSC 的信号元数据，记录输入级别、最少 K 线数、依赖指标和输出枚举。
- [x] 在图表 tooltip 和训练详情中展示信号的完整触发依据，包括参与笔、中枢、MACD/SMA 操作数和逐项命中条件，而不是只画“买一/买二/买三”。
- [x] 增加信号冲突规则测试：同日多信号全部保留，图表按日期与上下侧分配独立像素轨道，缩放重建时复用同一轨道偏移，交易决策不自动合并。

## P2：Signal → Event 组合层

- [x] 参考 CZSC `Signal / Event` 的 `all / any / not` 组合方式，增加教学用观察条件编排。
- [x] Event 只生成解释和候选提示，不直接替代用户手动买卖，也不修改现有撮合、T+1、费用和整手规则。
- [x] 保存每次操作当时满足和不满足的 Event 条件，供 AI 分析“看到了什么、做了什么”。
- [x] 增加可序列化训练方案：保存题目索引、未揭示 K 线、成本参数和完整窗口 SHA-256；历史详情可创建全新 session 原题重练，数据缺失或被篡改时拒绝重放。
- [x] 活跃训练支持显式放弃并随机重新开始；后端同步删除未完成 session，且不会生成训练记录。

## P2：回放、质量校验与诊断

- [x] 参考 CZSC 的逐根 replay 思路，提供开发模式下的结构变化时间线。
- [x] 增加 K 线质量校验：重复时间、乱序、OHLC 非法、成交量异常、交易日缺口和复权突变。
- [x] 图表支持点击分型、笔、中枢和买卖点后显示原始 K 线、确认 K 线与计算依据。
  - [x] 已增加向后兼容的图表检查事件、结构索引映射和训练/历史共用检查面板；盲训时不显示日期。
  - [x] 主价格轴对动态标记留白产生的小数边界做显示格式化，避免标签裁切成异常大数；计算边界精度不变。
  - [x] 已在生产构建中完成 `markPoint`、`markLine`、`markArea` 三类画布点击以及缩放后再次点击验收；B/S、分型、笔和中枢保持绑定对应 K 线。
- [x] 增加随机股票批量对照工具，逐根比较分型、笔、中枢和三类买卖信号，输出首个差异位置、结构数量与耗时且不写训练记录。可选差分运行时也固定到同一 Git revision：`uv run --with "czsc @ git+https://github.com/waditu/czsc.git@701e480a545004f945bb1721e510ae610ad90c4c" python -m scripts.compare_chan_czsc --count 5 --seed 42 --max-bars 800`。
- [x] 增加 AI 未配置、上游失败后重试和仅成功报告落盘测试；训练记录不会因 AI 失败被删除或覆盖。
- [x] 增加训练 JSON 存储原子替换与回读测试；分钟分区写入后若 DuckDB 视图刷新失败会显式返回失败原因。

## 暂不移植

- CZSC 数据源连接器：本项目已有设置驱动的数据源、分钟分区和 DuckDB 视图，继续复用现有契约。
- CZSC `Position`、权重回测与撮合：本项目已有现金百分比、持仓百分比、100 股整手、T+1 和费用口径。
- CZSC HTML 绘图：本项目已有统一 ECharts K 线、副图、缩放和历史详情组件，只参考交互，不引入第二套图表。
- CZSC 缓存目录与配置体系：避免绕开本项目仓库锁、原子写入和设置页面。

## 完成标准

- 同一固定输入与参考 CZSC 源码的分型、笔、中枢及三类买卖信号逐根一致。
- 无未来数据测试、真实样本测试、性能基准、后端定向测试和前端构建全部通过。
- 训练记录可追溯到算法来源、版本、信号 profile、数据级别及分钟覆盖状态。
- 页面清楚区分 CZSC 结构与 CZSC 原始信号，并明确当前固定上游提交不提供独立线段计算器。
