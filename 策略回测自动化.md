# 策略回测自动化

> 整份文件就是提示词。在**有 Windows shell 权限**的会话里（能跑 PowerShell / Git Bash，工作目录 `D:\project\tick-stock-panel`）整段粘贴即可开工。原先的「计划」和「提示词」两份文档已合并到本文件，其余同名文件作废。

你在 `D:\project\tick-stock-panel`（TickFlow，本地 A 股量化工作台，FastAPI + React，数据是 `data\` 下按日期分区的 Parquet）里接手一轮策略优化。上一轮的产物在 `.tmp-strategy-opt\`，先读 `bt.py`、`r1_out.json`~`r3_out.json`、`probe2.log`、`decile.log`，再动手。

任务：自动启停后端，对 `data\strategies\custom\` 下 7 个 `custom_sequoia_*.py` 策略做诊断、重写、寻优、步进验证，产出一份处置报告。

## 三件必须先知道的事

**这不是调参问题。** 6 个能跑的策略在 2004-2026 全区间无一盈利（年化 -0.1% ~ -21.1%），而且**零成本仍然 -4.1%/年**——亏损主因是入场条件负 alpha，不是交易成本、不是参数没调好。止盈档位、移动止损档位、拉长持有期这三类调整的差异全在噪声级。所以重心在阶段 2（补引擎能力）和阶段 4（按探针发现重写入场信号），不是撒网扫参。

**「去回测页面」等价于直接打 API。** 页面点一次「运行回测」就是发一次 `POST /api/backtest/strategy/run`，上一轮的 `mkbase.py` 专门用来把请求体对齐成和页面逐字段一致。走 API 能并发、能存档、能复现；走浏览器 CDP 慢且脆（上一轮的 `cdp.cjs`/`ui_run.js` 走过这条路）。阶段 8 保留一次「页面 vs API 逐位比对」做验证，通过之后不再开浏览器。

还有一条容易搞错的：**回测只认请求体里的 `overrides`，完全不读 `strategy_overrides\*.json`**（`app\backtest\strategy.py:1021` 是 `overrides = config.overrides or {}`）。所以那些 JSON 影响的是页面表单默认值、选股和监控，改它不会让任何回测数字变化；反过来说，每条基线的风控参数只存在于请求体里，`bt.py` 因此把全量请求体一起存档。

**回测结果后端不落库不落文件**（SSE job 表 5 分钟就清），每一轮必须自己写 JSON 存档到 `.tmp-strategy-opt-v2\`，否则重跑成本极高。

## 已确认的四个决定，不要再问

| 项 | 决定 | 对执行的影响 |
|---|---|---|
| 调整边界 | 允许改策略代码逻辑 | 原 7 个文件一个字不动，改造走新建 `custom_sequoia_<name>_v2.py`（`META["id"]` 加 `_v2`），永久保留可对照基线 |
| 排序口径 | Calmar（年化 / 最大回撤） | 所有排序、寻优 `objective`、步进 `objective` 统一填 `calmar` |
| 环境仓位 | 在引擎里重新实现五级仓位缩放 | 阶段 2 是引擎改动，需要回归保护，排在体检之前 |
| 区间与验证 | 近 10 年 + 步进验证 | 主区间 `2016-01-01` ~ `2026-09-02`；阶段 6 walk-forward 是硬门槛 |

## 上一轮交接状态（已完成本轮验证）

以下改动全部在**没有 shell 的会话里**完成，**未经任何运行验证**。文件都在，别重写，先跑。

引擎侧（阶段 2 的实现）：

- `backend\app\backtest\regime_alignment.py`：新增 `REGIME_LEVELS`、`normalize_regime_position_pct`（档位名拼错直接报错）、`build_regime_scale_series`（逐 bar 系数，T-1 对齐，首日退化为 1.0）
- `backend\app\backtest\engine.py`：`simulate_market_matrix` / `_simulate_portfolio_matrix` 新增 `regime_scale` 参数；逐 bar `bar_exposure_pct = max_exposure_pct * scale[t]`；超上限时按 `entry_score` 从低到高强制减仓，退出记 `exit_reason="regime_reduce"`；`regime_scale` 长度/有限性校验
- `backend\app\backtest\strategy.py`：`StrategyBacktestConfig.regime_position_pct`；`_load_regime_by_date` 抽取 + `_build_regime_scale`；日线与分钟两条 `run` 路径都接上；`mode="full"` 传档位直接报错
- `backend\app\api\backtest.py`：`StrategyBacktestRequest` 加字段并透传；`/strategy/stream`、`/optimize/stream`、`/walkforward/stream` 都接受 `regime_position_pct`（JSON 串），进 job_key，解析失败**报错而不是静默降级**；`/strategy/cancel` 补齐 `minute_fill`/`regime_filter`/`regime_position_pct` 以免算出的 key 失配
- `frontend\src\pages\backtest\StrategyBacktest.tsx`：`ExitReasonBadge` 加「降档减仓」标签

工具与测试：

- `backend\tests\backtest\test_regime_position_scaling.py`：10 个用例，含「五级全 1.0 ⇔ 不传字段逐位相同」的回归保护
- `tools\devctl.ps1`：`start` / `stop` / `restart` / `status` / `wait-ready`，两级就绪判定，`taskkill /F /T`，默认不抢占已有实例，`-FullStack` 另起前端
- `.tmp-strategy-opt-v2\bt.py`：端口 3018、区间 2016-01-01~2026-09-02、删掉 `mainline_priority`、基线不带 `regime_position_pct`、逐组落盘、存全量请求体
- `.tmp-strategy-opt-v2\clean_overrides.py`：清空操作覆盖，自校验（删完重算有效配置逐键比对，不一致自动回滚），默认 dry-run

策略侧（阶段 4 的参考实现，只有一个，其余等阶段 3 定）：

- `data\strategies\custom\custom_sequoia_ma_volume_v2.py`：反转入场（20 日动量 ≤ 0 且站上 ma60，无趋势确认）+ 内部低成交额/低换手/低波动/低动量四因子打分 + `basic_filter` 去掉三个前复权界与 `turnover_min` + 止盈与移动止损全拆、止损放宽到 -12%、持有 40 天
- `.tmp-strategy-opt-v2\r2_v2_configs.json`：`base` / `v2-entry-only` / `v2-full` 三格对照组，把总差异拆成「入场+打分」和「风控+过滤」两半

**你的前两件事**，顺序不能反：

1. `cd backend; uv run pytest tests/backtest -q` —— 全绿才说明阶段 2 的引擎改动没破坏既有结论
2. `.\tools\devctl.ps1 start; .\tools\devctl.ps1 wait-ready; .\tools\devctl.ps1 status; .\tools\devctl.ps1 stop` 走一轮 —— 确认启停可控再进阶段 1


## 上一轮已有的结论：不要重复这些实验

已证伪、不要再试：止盈档位（14%/30%/取消，差 ≤0.5pp）；移动止损档位（12%→20% 与直接关掉几乎无差别）；`mainline_priority` 开关（0.15pp，噪声级）；恢复策略自带打分（反而差 1pp）；放宽止损 + 持有拉到 120 天（更差）。

已验证有效：**拆掉 -6% 移动止损**（它占 51.6% 退出、均亏 -2.49%，是在噪声里割自己，年化 -15.8% → -11.2%）；低动量/低量/低位方向打分优于高动量打分。

探针发现的正 edge 方向（原始结论在 `probe2.log`、`decile.log`、`custom_quiet_lowpos.py` 的 docstring 里，阶段 4 就靠这三条）：

- A 股主板**短期反转优于动量，追涨是负 edge**。当前「金叉+放量+上涨」的 20 日横截面超额是 **-0.18%**（加上升趋势过滤 -0.20%）；20 日动量 >+20% 是 -0.94%，5 日 >+10% 是 -0.99%。唯一 20 日为正的形态是「20 日动量 ≤0 且站上 ma60」**+0.026%**。反转类多在 5 日为正、20 日转负。
- 十分组 60 日前瞻**全部因子单调负斜率**，即**低成交额 / 低换手 / 低波动 / 低乖离 / 低动量占优**：turnover20 首末价差 -5.26pp、amount20 -4.26pp、vol20 -3.47pp、close/ma60 -2.27pp、r20 -2.23pp。Amihud illiq 是唯一正斜率 +2.27pp（与低成交额高度重叠）。
- `basic_filter` 的 `price_min` / `market_cap_min` 作用在**前复权 close** 上，会系统性反向剔除未来大牛股。实测 2005-06-30 通过者到末期涨幅中位 **2.67x**、被剔除者 **6.46x**，且只有 72/1086 通过。这是唯一有希望的结构性发现，上一轮没验证完（`r4.json` 8 组全挂在后端崩溃上），本轮要补上。

## 硬约束，违反会出事

- **不要跑 `uv sync`**：它会卸掉单独装的插件包（MooTDX 等），`prod.sh:126` 有说明。缺依赖用 `uv pip install <pkg>`。
- **`taskkill` 必须带 `/T`**：后端会派生回测子进程，不带 `/T` 会留孤儿进程占端口和数据锁。
- **同一个 `DATA_DIR` 只能有一个后端实例**（`MiningProcessLock`）。启动前先 `status`，别把已有实例挤掉；结束时一定收尾。
- **不要加 `--reload`**：文件监听会在改策略文件时重启后端，正在跑的回测直接丢。
- **新写的策略文件对已在跑的后端不可见**：策略注册表只在 `StrategyEngine.__init__` 里 `_load_all` 一次（`app\strategy\engine.py:251`），之后只有 `reload()` 会重扫目录。所以阶段 4 落盘 `_v2` 文件之后，必须打一次 `POST /api/strategy/reload`（`app\api\strategy.py:1126`，原子热重载，任一策略失败会保留上一版并报错），否则回测会报「策略不存在」，而你会以为是文件写错了。这个接口**不在** 401 白名单里，设了访问密码就得带 `tf_session`：
  `Invoke-RestMethod -Method Post -Uri http://127.0.0.1:3018/api/strategy/reload`（无密码）；有密码时加 `-WebSession $s` 或 `-Headers @{Cookie="tf_session=<cookie>"}`。重启后端也可以，但会白等一次指标预热。
- **重活并发上限 2**（`HeavyJobLimiter(capacity=2)`），且预热本身占一个槽。上一轮 8 组并发在 54 秒内把后端打崩（`r4.json` 全挂）。
- **所有对比只用 `mode="position"`**：`full` 是候选级统计口径，`position` 才是组合口径，两者数字不可混比；传了环境档位再用 `full` 会直接报错。
- **密钥在 `data\user_data\secrets.json`**（优先级高于 `.env`）。只允许列 key 名，不读值、不回显、不写进任何存档文件。
- **设了访问密码时 `/api/*` 全部 401**（`app\main.py:421` 的鉴权中间件，白名单只有 `/health`、`/api/health`、`/api/auth/`、`/openapi.json`、`/docs`、`/redoc`）。`/api/data/status` 和所有回测接口都在管控内，没 `tf_session` cookie 就一路 401。先 `POST /api/auth/login` 拿 cookie，再用 `TF_SESSION=<cookie>` 跑 `bt.py`；`devctl.ps1 status` / `wait-ready` 会主动探测并提示这件事。
- **原 7 个策略文件字节不动**。所有改造走 `_v2` 新文件。
- 每轮结果**自己存 JSON** 到 `.tmp-strategy-opt-v2\`，文件名带轮次和策略 id，顺手记下请求体，保证可复现。

## 阶段 0：启停脚本（已验证）

Windows PowerShell 5.1 下，应用本身会打开 `data\backend.log`，不能再把控制台 stdout 重定向到同一文件，否则会触发文件锁冲突。当前 `tools\devctl.ps1` 将控制台输出写入 `data\devctl-stdout.log` 与 `data\devctl-stderr.log`；应用日志仍保留在 `data\backend.log`。

`tools\devctl.ps1` 已落盘：`start` / `stop` / `restart` / `status` / `wait-ready`，`-FullStack` 额外起前端 Vite。后端启动方式为工作目录 `backend`、`.\.venv\Scripts\python.exe -m uvicorn app.main:app --env-file ..\.env --host 127.0.0.1 --port 3018`，后台启动，stdout/stderr 分别重定向到 `data\backend.log` 与 `data\backend.err.log`，pid 记在 `data\devctl-backend.pid`。

两个设计要点，改它之前先知道为什么：就绪判定分两级（先 `GET /health` 60 秒内 200，再轮询 `GET /api/data/status` 到 `indicators_ready == true`），只看 `/health` 会在指标还没预热完时就开跑，第一轮回测必然报数据错；`stop` 除了按 pid 文件杀，还按端口占用兜一遍，因为 pid 文件可能过期（手工启过 / 上次没收尾），只认 pid 文件会留孤儿占着端口和数据锁。默认绑 `127.0.0.1` 不对外，退出码 0 = ok / 1 = 失败 / 2 = 起来了但没就绪。

**验收**：`start` → `wait-ready` → `status` → `stop` 一轮走通，`status` 在停掉后报告未运行，端口 3018 无残留监听。

## 阶段 1：基线锚点与清理无效覆盖

`.tmp-strategy-opt-v2\bt.py` 已按新口径改好（端口 3018、区间 2016-01-01~2026-09-02、删掉 `mainline_priority`、基线**不带** `regime_position_pct`、按 calmar 排序、逐组落盘并存全量请求体）。跑法 `python bt.py <configs.json> [out.json]`，配置写错会在提交前就炸而不是跑一半才发现。

覆盖文件在 **`data\user_data\strategy_overrides\*.json`**（不是 `data\strategies\custom\`）。里面有一批与策略默认值逐字相同的空操作覆盖，留着只会让人误判「已经调过了」。用 `.tmp-strategy-opt-v2\clean_overrides.py` 清，别手删：覆盖的语义是**键在不在**而不是值是不是 `null`（`app\backtest\strategy.py:2380`），手删很容易把「显式关掉某个风控」当成空操作删掉。脚本自己从策略 `.py` 里 `ast` 解析默认值算出哪些键是空操作，删完重算有效配置逐键比对，不一致就回滚该文件并非零退出；默认 dry-run，`--apply` 才写盘，备份进 `.tmp-strategy-opt-v2\json-backup\`。

预期结果（对不上就先停下来查，不要 `--apply`）：`high_tight_flag` 与 `private_placement` 整个文件删掉；`ma_volume` 只剩 `take_profit: 0.14`（默认 0.16）和 `scoring_replace: true`（**它是真改动：`scoring:{}` + replace 等于关掉策略自带打分，上一轮实测恢复自带打分反而差 1pp**）；`turtle_trade` 只剩放宽后的 `basic_filter` 三项和 `params` 里的 `breakout_buffer: 0` / `amount_min_yi: 0.5`；`rps_breakout` / `limit_up_shakeout` / `uptrend_limit_down` 各自去掉与默认相同的 `boards` 与 `null` 档位，风控保留。后端读 override 带 (mtime, size) 签名缓存且每次调用都 stat，所以清完不用重启。

`custom_sequoia_private_placement` 依赖本地没有的定增数据源，直接标注「需数据源，本轮不参与」，不要花时间在它上面。

**验收**：6 个可跑策略各出一条 2016-2026 基线，存档 `r0_baseline.json`，字段至少含 annual_return / max_drawdown / calmar / trades / win_rate / exit_reason 分布。

## 阶段 2b：跑通引擎改动的测试

`backend\tests\backtest\test_regime_position_scaling.py` 已写好 10 个用例，跟随 `test_engine_portfolio.py` 的写法（`_panel` / `_mask` 造面板，`BacktestEngine(repo=None)`）。覆盖：五级全填 `1.0` 与完全不传字段**逐位相同**（回归保护，`x * 1.0 == x` 且蒙特卡洛已固定 `default_rng(42)`，只排除纯计时的 `stats["statistics_ms"]`）；弱市档位 0 时一笔新仓都不开且计入 `buy_exposure`；档位下调时按 `entry_score` 从低到高减仓、退出记 `regime_reduce` 且不污染信号腿统计；`regime_scale` 长度不符 / 含 NaN·Inf·负值报错；`normalize_regime_position_pct` 对档位名拼错 / 非数值 / 越界报错；`build_regime_scale_series` 的 T-1 对齐、首日中性、未知状态退化为 1.0、内部缺口 fail-closed。

注意 `simulate_portfolio` 不接受 `regime_scale`（只有 `simulate_market_matrix` 有），所以测试里用 `_matrix()` 复刻了 `simulate_portfolio` 内部的 `build_market_matrix` 口径再调矩阵入口。

**验收**：`cd backend && uv run pytest tests/backtest -q` 全绿（含既有用例，不许 xfail 掩盖）。挂了先看是不是断言里的手算份数/敞口与引擎实际取整口径不一致，那是测试的问题；`test_all_ones_ladder_is_bit_for_bit_identical_to_no_ladder` 挂了才是引擎的问题，且意味着既有基线结论全部作废。


## 阶段 3：逐策略体检

对 6 个策略各做一次归因，不是再刷一遍收益数字，而是回答：入场信号的横截面超额是多少（沿用探针口径）、退出原因分布里哪一类在吃掉收益、`basic_filter` 剔掉了多少标的、成交额/换手分位落在十分组的哪一档。

**验收**：一张 6 行的诊断表 + 每个策略一句「主因」结论，存 `r1_diagnosis.json`。哪个策略值得改造、哪个直接放弃，在这一步定。

## 阶段 4：按探针发现重写入场（本轮重点）

先读 `backend\app\strategy\prompts\strategy-guide.md`，再照 `custom_sequoia_ma_volume.py` 的骨架，为选中的策略各建一个 `data\strategies\custom\custom_sequoia_<name>_v2.py`。四条改造方向直接来自上面三条探针发现：

- **反转替代追涨**：把「金叉 + 放量 + 上涨」换成「20 日动量 ≤ 0 且站上 ma60」这一类回调形态，不要要求上升趋势确认。
- **打分转向低位低量**：策略内部用 `_row_rank01` 做横截面分位，方向取低成交额 / 低换手 / 低波动 / 低乖离，而不是高动量。
- **`basic_filter` 去掉 `price_min` / `price_max` / `market_cap_min`**：它们作用在前复权 close 上，会系统性剔除未来大牛股。流动性约束改用成交额分位这类不受复权影响的口径。
- **持有 20~60 天，不挂 -6% 移动止损**：上一轮已证明它在噪声里割自己。

**已落盘一个参考实现**：`custom_sequoia_ma_volume_v2.py`（现已完成本轮真实回测）。选它打头是因为 6 个策略里只有它的诊断已经齐了——入场超额 -0.18%、打分三项全压在负 edge 方向、`basic_filter` 三个前复权界都在、且 -6% 移动止损就挂在它身上，四条改造方向逐条对得上。其余 5 个未新增 `_v2`，阶段 3 结果不足以支持改造。

它比计划多做了一件事：**连 `turnover_min` 一起去掉**。`turnover_min: 0.5` 砍掉的正是十分组里表现最好的低换手一侧（turnover20 首末价差 -5.26pp，是五个因子里最陡的），留着它等于一边按低换手打分一边把低换手筛掉。换手上限挪进策略内部做参数（默认 10%），流动性只留 `amount_min`（不受复权影响）。

**一个会静默吃掉改动的坑**：回测只读请求体里的 `overrides`，而 `bt.py` 的 `BASE` 每次都发全量风控块（`take_profit 0.14` / `trailing_stop -0.06` / `max_hold_days 25`）。不在 patch 里改掉它们，`_v2` 文件里的 `TAKE_PROFIT = None` / `TRAILING_STOP = None` / `MAX_HOLD_DAYS = 40` 就完全不生效——跑出来的还是基线风控，而且不会有任何报错。`basic_filter` 同理：请求体里的键会 `.update()` 盖在策略 `BASIC_FILTER` 上，要关掉某个界得显式传 `null`（`null` 在请求体这条路上是「界不生效」，与保存版 JSON 的语义不同）。

现成的对照组在 `.tmp-strategy-opt-v2\r2_v2_configs.json`，三格设计：`base-ma_volume`（基线锚点，与 `r0_baseline` 同名组应逐位一致，顺便当一次复现校验）、`v2-entry-only`（只换入场与打分，风控和 `basic_filter` 保持基线原样）、`v2-full`（完整提案）。这样总差异能拆成「入场+打分」和「风控+过滤」两半，而不是只得到一个混在一起的数。跑法 `python bt.py r2_v2_configs.json r2_v2_vs_base.json`。

**验收**：每个 `_v2` 与自己的基线同区间同参数对打，calmar 提升写进 `r2_v2_vs_base.json`。**没提升就如实记录没提升**，不要回头去调参凑数。


## 阶段 5：定向寻优，不撒网

只对阶段 4 里 calmar 转正或明显改善的 `_v2` 做寻优，`objective` 填 `calmar`。网格必须小而有理由：每个策略最多 2 个维度、每维最多 4 档，且只选阶段 3 诊断指向的维度（典型是持有期与打分权重）。环境仓位档位在这一步作为整轮固定配置进出各跑一次（`regime_position_pct` 不参与网格），看它是否真的改善回撤。

**验收**：`r3_optimize.json`，含每组参数的 IS 结果与网格全貌；明确写出「最优点是否贴在网格边界上」（贴边界说明网格没框住，要么扩、要么承认该维度无效）。

## 阶段 6：步进验证，硬门槛

`train_days=252` / `test_days=63` / `step_days=63`，`objective=calmar`，主区间 2016-01-01 ~ 2026-09-02。这是本轮唯一有资格支撑「策略可用」结论的检验：阶段 5 的 IS 最优点如果在 OOS 段站不住，就是过拟合，不许当成结论交付。

**验收**：`r4_walkforward.json`，逐段列出 IS 与 OOS 的 calmar / 年化 / 回撤，并给出 OOS 段的胜段比例。

## 阶段 7：`basic_filter` 结构性验证（补上一轮的坑）

上一轮 8 组并发把后端打崩、`r4.json` 全挂的那件事，本轮**串行**重做（并发上限 2，预热占一个槽，所以实际最多 1 组在跑）。对照组设计：原 `basic_filter` vs 去掉 `price_min` vs 去掉 `market_cap_min` vs 两者都去，其余参数完全一致。

**验收**：`r5_basic_filter.json`，四组结果齐全，明确回答「前复权价格/市值下限是否系统性伤害长期收益」。

## 阶段 8：页面与 API 一致性 + 收尾

挑一个 `_v2` 策略，在回测页面手点一次，与同参数的 API 调用**逐位比对**（除 `statistics_ms`）。通过之后不再开浏览器。

比对时以**页面实际发出的请求体**为准（devtools Network 里抄），不要照着页面表单显示的数字反推：`GET /api/strategy/{id}` 返回的 `take_profit` / `trailing_stop` / `trailing_take_profit_*` 取的是策略默认值（`app\api\strategy.py:208-211` 走 `getattr(s, ...)`），**没有应用已保存的 override**，而同一个响应里的 `stop_loss` / `max_hold_days` 却应用了。页面的风控表单是用这个响应初始化的，所以它可能和 `strategy_overrides\*.json` 里存着的值不一致。这看着像个真 bug，但**本轮不改**——改了会动前端行为，与「先拿到可信基线」的目标冲突，记进报告即可。

然后 `devctl.ps1 stop` 收尾，确认端口和数据锁都释放。

**验收**：比对结论写进最终报告；`status` 报告未运行。

## 算力预算

并发固定 2 且预热占一个槽，所以按「同时最多 1 个有效回测」估时间。单次 2016-2026 全市场日线回测按分钟级耗时估，6 个策略一轮基线就是小时量级。阶段 5 的网格务必先算清总组数（策略数 × 网格点数），超过 40 组就砍维度——上一轮的教训是并发贪心直接把后端打崩，重跑成本远高于省下来的时间。

## 完成标准

### 结果落盘与参数方向（新增硬约束）

- 每次回测必须由 `tools\backtest_runner.py` 顺序提交；响应返回后立即写入一个独立 JSON，并追加一行 `.tmp-strategy-opt-v2\backtest.log`，然后才能开始下一次请求。
- 每条记录必须包含完整请求体、原始响应或明确错误、开始/结束时间和 `direction`。失败请求也要落盘，不能用空结果代替。
- 页面/API 一致性核验的页面实测与 API 重放也必须追加到同一日志，标记为 `page_api_verification`；该核验只确认一致性，不推动参数变更。
- 参数调整只能逐步正向：只有实际响应同时满足 `calmar` 上升且最大回撤绝对值不变差，才输出 `advance_one_step`；指标缺失输出 `no_change_metrics_missing`，负向或混合结果输出保持/回滚建议。
- 若 `baseline_of` 指向尚未成功完成的记录，输出 `no_change_baseline_missing`，不得把该次结果当作基线或寻优结论。
- 禁止手填、估算或复制收益数字。API 不可用、worker 被权限阻断或数据缺失时，只记录错误并停止该方向，不生成基线/寻优结论。
- Windows 运行示例：`python tools\backtest_runner.py <configs.json> --out-dir .tmp-strategy-opt-v2`。需要检查请求体而不触发回测时加 `--dry-run`。

上一轮 smoke 记录仍保留在 `generated\backtest-runner-live-smoke\`，其中 HTTP 500 不是本轮收益结论；本轮正式结果见下方执行记录。前端 `tsc -b` 已通过，Vite 构建曾被沙箱 `spawn EPERM` 阻断。

## 本轮实际执行记录（2026-09-03）

- Windows PowerShell 5.1 环境下，`multiprocessing.Queue` 的匿名管道创建仍被当前受限权限拒绝。服务端新增了仅在捕获 `PermissionError` 时启用的同步回退，响应元数据标记 `isolation_mode=inline_permission_fallback`；正常环境仍使用 spawn worker。该回退有专门回归测试，未改变回测业务逻辑。
- 六个可运行策略已按 `2016-01-01`~`2026-09-02`、`mode=position`、`matching=open_t+1` 串行完成真实基线，完整请求/响应分别存于 `.tmp-strategy-opt-v2/001-*.json` 至 `006-*.json`，汇总为 `r0_baseline.json`。
- 基线实际 Calmar：`high_tight_flag=0.01`、`limit_up_shakeout=-0.15`、`ma_volume=-0.28`、`rps_breakout=-0.36`、`turtle_trade=-0.39`、`uptrend_limit_down=-0.10`。`private_placement` 因本地缺少定增公告数据源标记为 `needs_data_source`，没有生成收益数字。
- `ma_volume_v2` 三组对照真实完成：`v2-entry-only` 为 `-0.18`（相对 `-0.28`，回撤绝对值 `0.8304` 对 `0.9615`），`v2-full` 为 `0.15`（回撤绝对值 `0.4123`），两组均满足 Calmar 上升且回撤不变差，运行器输出 `advance_one_step`，存档为 `r2_v2_vs_base.json`。
- 小网格仅测试 `mom_min` 与持有期两个维度共四点，最佳实际 Calmar `0.11`，低于 `v2-full` 锚点 `0.15`，因此全部保持/回滚，不扩大网格；存档为 `r3_optimize.json`。
- 阶段 6 对 `v2-full` 锚点按 `train/test/step=252/63/63` 完成 57 折 walk-forward，0 折跳过；OOS 正 Calmar 31/57（`0.5439`），复合 OOS 收益 `0.1151`。结果是描述性证据，不把它扩大为普遍可用性结论，详见 `r4_walkforward.json`。
- `basic_filter` 四组真实结果：原过滤 `-0.28/-0.9615`；去价格下限 `-0.29/-0.9685`；去市值下限 `-0.33/-0.9843`；两者都去 `-0.33/-0.9839`（Calmar/最大回撤）。本区间未证实去除下限有益，结论为保持原过滤，存档为 `r5_basic_filter.json`。
- 每一次请求均由 `tools\backtest_runner.py` 串行提交，响应后立即写独立 JSON 并追加 `.tmp-strategy-opt-v2\backtest.log`；未用手填、估算或复制收益数据。

一份处置报告，对 7 个策略逐个给出结论：**保留 / 改造为 `_v2` / 放弃 / 需数据源**，每条结论必须挂上支撑它的存档文件与数字。报告里要明确区分「已跑通验证的」和「仅代码就绪未验证的」。

最后一条：如果走完阶段 6 仍然没有任何策略在 OOS 段站住，那就如实写「近 10 年内这 7 个策略的入场逻辑均无正 edge」，并把方向转向 `custom_quiet_lowpos` 那条低位低量思路继续做研究。**不要为了交出一个正收益而反复扫参凑数。**
